*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Native build artifacts (libmpv + ffmpeg + libplayer) built via mpv-android buildscripts
/app/src/main/jniLibs/
/app/src/main/obj/

# Source-built AAR placed by F-Droid build (useLocalLibmpv)
/app/libs/

# Release signing keystores (credentials live in ~/.gradle/gradle.properties)
*.keystore
*.jks

# Local env file with signing credentials (source it before building: `set -a; . ./.env; set +a`)
.env
